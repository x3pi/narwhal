# NGUYÊN NHÂN GỐC RỄ: TRANSACTION HASH KHÁC NHAU

## VẤN ĐỀ

Transaction hash được tính **KHÁC NHAU** giữa Worker và Node:

- **Worker Hash:** `705996768350a7516e1cf23dd32acf62c635075bc79ab57282c83999237f5f0b`
- **Node Hash (tx[0]):** `9bce549a13c29981a620d7996cb3e0cd82c221804b4587124b417b02caac9c97`
- **Node Hash (tx[1]):** `1defce68f41b7ae2853b75746231c5cffc5a0290344257c38c9f760150a6e6fd`

**Kết quả:** Transaction đã được thực thi (trong block height 25706), nhưng hash không khớp nên không thể tìm thấy bằng hash từ worker.

---

## PHÂN TÍCH

### 1. **Batch Đã Được Commit và Gửi Đến UDS**

```
[BATCH TRACK] Node ID 2 SUCCESSFULLY sent block height 25706 (leader round 51412) to UDS containing 2 transactions from 1 batches. Batches: [FTKVmdSKU7Eje1OJnNQGHSPRf5Gsm0OyIioDtj1mR2o=]
```

### 2. **Transaction Đã Có Trong Block**

Block height 25706 chứa **2 transactions** từ batch `FTKVmdSKU7Eje1OJnNQGHSPRf5Gsm0OyIioDtj1mR2o=`:

- **tx[0]:** `Hash=9bce549a13c29981a620d7996cb3e0cd82c221804b4587124b417b02caac9c97`
- **tx[1]:** `Hash=1defce68f41b7ae2853b75746231c5cffc5a0290344257c38c9f760150a6e6fd`

### 3. **Nguyên Nhân: Hash Tính Khác Nhau**

**Worker:** Sử dụng `calculate_transaction_hash()` từ `transaction_logger.rs`
- Hash từ `TransactionHashData` (protobuf encoded)

**Node:** Sử dụng `calculate_transaction_hash()` từ `node/src/main.rs`
- Hash từ `TransactionHashData` (protobuf encoded)

**Nhưng:** Transaction payload có thể khác nhau do:
- **Length prefix:** Node strip 8-byte length prefix trước khi parse
- **Encoding:** Worker và Node có thể encode khác nhau

---

## CHI TIẾT KỸ THUẬT

### Worker Hash Calculation

```rust
// worker/src/transaction_logger.rs
pub fn calculate_transaction_hash(tx: &Transaction) -> Vec<u8> {
    let hash_data = TransactionHashData { ... };
    let mut buf = Vec::new();
    hash_data.encode(&mut buf); // Protobuf encode
    let hash = Keccak256::digest(&buf);
    hash.to_vec()
}
```

### Node Hash Calculation

```rust
// node/src/main.rs
pub fn calculate_transaction_hash(tx: &Transaction) -> Vec<u8> {
    let hash_data = TransactionHashData { ... };
    let mut buf = Vec::new();
    hash_data.encode(&mut buf); // Protobuf encode
    let hash = Keccak256::digest(&buf);
    hash.to_vec()
}
```

**Vấn đề:** Transaction payload trong batch có **8-byte length prefix**, nhưng khi parse trong Node, length prefix được strip:

```rust
// node/src/main.rs
const LENGTH_PREFIX_SIZE: usize = 8;
let tx_payload = if tx_data.len() > LENGTH_PREFIX_SIZE {
    let payload = tx_data[LENGTH_PREFIX_SIZE..].to_vec(); // STRIP LENGTH PREFIX
    // ...
}
```

**Kết quả:** Transaction hash được tính từ payload **SAU KHI STRIP**, không phải từ payload gốc trong batch.

---

## GIẢI PHÁP

### 1. **Sửa Hash Calculation trong Node**

Đảm bảo hash được tính từ **cùng payload** như Worker:

```rust
// node/src/main.rs
// Tính hash từ payload SAU KHI STRIP length prefix
let tx_hash = Keccak256::digest(&tx_payload).to_vec();
```

**Nhưng:** Worker tính hash từ Transaction object (protobuf), không phải từ raw payload.

### 2. **Sửa Hash Calculation trong Worker**

Tính hash từ raw payload (sau khi strip length prefix) thay vì từ Transaction object:

```rust
// worker/src/transaction_logger.rs
// Tính hash từ raw payload (sau khi strip length prefix)
let tx_hash = Keccak256::digest(&tx_payload_after_strip).to_vec();
```

### 3. **Sửa Hash Calculation trong Cả Hai**

Đảm bảo cả Worker và Node tính hash từ **cùng nguồn dữ liệu**:
- Cùng strip length prefix
- Cùng parse Transaction
- Cùng encode TransactionHashData

---

## KẾT LUẬN

**Transaction đã được thực thi** trong block height 25706, nhưng:

1. **Hash không khớp** do Worker và Node tính hash từ payload khác nhau
2. **Worker hash:** Từ Transaction object (protobuf)
3. **Node hash:** Từ payload sau khi strip length prefix

**Giải pháp:** Cần đảm bảo cả Worker và Node tính hash từ **cùng nguồn dữ liệu** để hash khớp nhau.

---

## CÁCH KIỂM TRA

### 1. **Tìm Transaction Bằng Node Hash**

```bash
# Tìm transaction bằng hash từ Node
grep -E "Hash=9bce549a13c29981a620d7996cb3e0cd82c221804b4587124b417b02caac9c97|Hash=1defce68f41b7ae2853b75746231c5cffc5a0290344257c38c9f760150a6e6fd" benchmark/logs/primary-*.log
```

### 2. **So Sánh Payload**

```bash
# So sánh payload giữa Worker và Node
grep -E "705996768350a7516e1cf23dd32acf62c635075bc79ab57282c83999237f5f0b|9bce549a13c29981a620d7996cb3e0cd82c221804b4587124b417b02caac9c97" benchmark/logs/*.log
```

### 3. **Kiểm Tra Length Prefix**

```bash
# Kiểm tra xem payload có length prefix không
grep -E "BATCH PROCESSING.*FTKVmdSKU7Eje1OJnNQGHSPRf5Gsm0OyIioDtj1mR2o=" benchmark/logs/primary-*.log | grep "original.*bytes.*after strip"
```

